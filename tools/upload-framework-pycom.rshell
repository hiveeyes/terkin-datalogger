# Install libraries to MicroPython target device using "rshell".

# Upload lib folder
rsync --mirror --verbose ./lib /flash/lib

mkdir /flash/lib/terkin
rsync --mirror --verbose terkin /flash/lib/terkin
