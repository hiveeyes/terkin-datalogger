# Install libraries to MicroPython target device using "rshell".

mkdir /flash/terkin
rsync --mirror --verbose terkin /flash/terkin

# Reset machine after upload
repl pyboard import machine~
repl pyboard machine.reset()
