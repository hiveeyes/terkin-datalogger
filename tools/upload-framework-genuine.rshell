# Install libraries to MicroPython target device using "rshell".

# Upload lib folder
mkdir /pyboard/lib
rsync --mirror --verbose ./lib /pyboard/lib

mkdir /pyboard/lib/terkin
rsync --mirror --verbose terkin /pyboard/lib/terkin
