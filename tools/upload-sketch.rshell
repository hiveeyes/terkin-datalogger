# Install Hiveeyes MPY data logger program
# to MicroPython target device using "rshell".

# Upload application library assets
rsync ./lib /flash/lib

# Upload main application assets
cp boot.py /flash
cp main.py /flash
cp settings.py /flash


# Just drop into REPL after upload
# repl

# Reset machine after upload
repl pyboard import machine~
repl pyboard machine.reset()
