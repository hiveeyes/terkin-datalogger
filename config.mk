## '?=' means: set if not already set
## so any settings on the command line will overwrite the settings here (or you remove the '?')
MPY_VERSION ?= 1.12
MPY_TARGET ?= bytecode
MCU_PORT ?= /dev/ttyS3
